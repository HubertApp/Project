[project]
name = "hubert-e2e"
version = "0.1.0"
description = "Parcours end-to-end du back Hubert, joué étape par étape contre la gateway"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "rich>=13.7",
]

[tool.uv]
package = false
